# <%= platformName %>

<%= platformName %> customization scripts.

## Index

<% categories.forEach(category => { %>
[<%= category %>](#<%= category.toLowerCase().replaceAll(' ', '-') %>)
<% Customizations.filter(a => a.categories.includes(category)).forEach(customization => { %> - [<%= customization.name %>](#<%= customization.name.toLowerCase().replaceAll(' ', '-') %>)
<% }) %>
<% }) %>

<% categories.forEach(category => { %>
# <%= category %>


<% Customizations.filter(a => a.categories.includes(category)).forEach(customization => { %>
## <%= customization.name %>
<%= customization.description == 'No description.' ? '' : customization.description %>

<% customization.commitScripts.forEach(commitScript => {%>
```<%- commitScript.scriptInterpreter %>
<%- commitScript.scriptContents %>
```
<% }) %>
<details>
    <summary>Revert script</summary>

    <% customization.uncommitScripts.forEach(uncommitScript => {%>
```<%- uncommitScript.scriptInterpreter %>
<%- uncommitScript.scriptContents %>
```
    <% }) %>
</details>  
<% if (customization.notes.length != 0) {%>
### Notes
<% customization.notes.forEach(note => {%>
- <%- note %>
<% }) %>
<%}%>
<% }) %>

<% }) %>
