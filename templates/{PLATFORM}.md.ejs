# <%= platformName %>


<% Customizations.forEach(customization => { %>
## <%= customization.name %>
<%= customization.description == 'No description.' ? '' : customization.description %>

<% customization.commitScripts.forEach(commitScript => {%>
```<%- commitScript.scriptInterpreter %>
<%- commitScript.scriptContents %>
```
<% }) %>
### Revert Changes
<% customization.uncommitScripts.forEach(uncommitScript => {%>
```<%- uncommitScript.scriptInterpreter %>
<%- uncommitScript.scriptContents %>
```
<% }) %>
<% if (customization.notes.length != 0) {%>
### Notes
<% customization.notes.forEach(note => {%>
- <%- note %>
<% }) %>
<%}%>
<% }) %>

